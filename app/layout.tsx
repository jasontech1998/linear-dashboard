import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Enhanced Linear Dashboard Clone",
  description: "A Linear-inspired dashboard with advanced due date indicators for efficient task management",
  keywords: "project management, kanban, task tracking, due dates, Linear clone",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
